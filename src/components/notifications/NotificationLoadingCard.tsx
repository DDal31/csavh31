import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export const NotificationLoadingCard = () => (
  <Card className="w-full">
    <CardHeader>
      <CardTitle>Chargement...</CardTitle>
    </CardHeader>
  </Card>
);